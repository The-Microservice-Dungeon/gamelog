/**
 * We define some Kafka Interceptors to make it easier for us to debug issues
 * in our service. These are found here.
 *
 * Note that every Interceptor defined here MAY modify the received record
 */
package com.github.tmd.gamelog.adapter.event.kafka.interceptor;